<?php 

$users = users::find("");

?>
{{ form("session/changepassword") }} 

    {{ password_field("oldpassword", "class": "form-control", "placeholder": "Password")}}
    {{ password_field("newpassword", "class": "form-control", "placeholder": "Password")}}
    {{ password_field("newpassword1", "class": "form-control", "placeholder": "Password")}}
    {{ submit_button("changepassword", "class": "btn btn-primary") }}

        <input type="hidden" name="<?php echo $this->security->getTokenKey() ?>"
        value="<?php echo $this->security->getToken() ?>"/>
        
{{ end_form() }}
